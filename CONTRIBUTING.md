# 贡献指南 (Contributing Guide)

👋 欢迎来到 **openai-apis** 项目！

首先，感谢您有兴趣为本项目做出贡献！开源社区的繁荣离不开像您这样的贡献者。无论是修复 bug、改进文档、添加新功能，还是仅仅提出建议，您的每一份贡献都非常有价值。

为了确保我们能够高效地合作，并保持代码库的高质量，请在贡献之前花一点时间阅读以下指南。

## 🤝 行为准则 (Code of Conduct)

我们致力于提供一个友好、安全和包容的环境。请在参与本项目时保持尊重和礼貌。

## 🛠️ 开发环境准备 (Getting Started)

### 前置要求

- **Node.js**: 本项目使用 Node.js (推荐 v18+，项目配置了 Volta，建议使用)。
- **包管理器**: 本项目包含 `pnpm-lock.yaml`，建议使用 [pnpm](https://pnpm.io/) 进行依赖管理。

### 步骤

1. **Fork 本仓库**：点击右上角的 "Fork" 按钮，将项目复制到您的 GitHub 账户。
2. **克隆仓库**：
   ```bash
   git clone https://github.com/YOUR_USERNAME/openai-apis.git
   cd openai-apis
   ```
3. **安装依赖**：
   ```bash
   pnpm install
   # 或者
   npm install
   ```

## 💻 开发流程 (Development Workflow)

### 1. 创建分支

请不要直接在 `master` 或 `main` 分支上进行开发。为您的更改创建一个新的分支：

```bash
git checkout -b feature/your-feature-name
# 或者
git checkout -b fix/your-bug-fix
```

### 2. 代码规范与风格

本项目使用 **TypeScript** 编写，并配置了 **ESLint** 和 **Prettier** 来保持代码风格一致。

- **Lint 检查**：
  ```bash
  npm run lint
  ```
- **格式化**：
  提交代码时，`lint-staged` 会自动运行 Prettier 格式化暂存区的文件。

### 3. 运行测试

在提交代码之前，请确保所有测试都能通过，并为您添加的新功能编写测试用例。

```bash
npm test
```

### 4. 构建项目

确保您的代码可以成功构建：

```bash
npm run build
```

## 📝 提交规范 (Commit Guidelines)

本项目严格遵循 **[Conventional Commits](https://www.conventionalcommits.org/)** 规范。我们使用了 `commitlint` 和 `husky` 来在提交时强制检查 commit message 的格式。

### 格式要求

```
<type>(<scope>): <subject>
```

### 常用 Type

- **feat**: 新增功能 (Features)
- **fix**: 修复 Bug (Bug Fixes)
- **docs**: 文档变更 (Documentation)
- **style**: 代码格式调整，不影响逻辑 (Styles)
- **refactor**: 代码重构 (Code Refactoring)
- **perf**: 性能优化 (Performance Improvements)
- **test**: 测试相关 (Tests)
- **chore**: 构建过程或辅助工具的变动 (Chores)
- **revert**: 回滚代码

### 示例

- ✅ `feat(core): add function calling support`
- ✅ `fix(types): resolve role type mismatch`
- ❌ `update code` (描述不清，且不符合规范)

如果您的提交信息不符合规范，`git commit` 将会被拦截。

## 📮 提交 Pull Request (PR)

1. **Push 代码**：将您的分支推送到您的 Fork 仓库。
   ```bash
   git push origin feature/your-feature-name
   ```
2. **创建 PR**：在 GitHub 上发起 Pull Request，指向本仓库的 `main` 分支。
3. **描述变更**：
   - 清晰地描述您所做的更改。
   - 如果修复了某个 Issue，请在描述中引用它（例如：`Closes #123`）。
   - 附上相关的截图或测试结果（如果有）。
4. **等待 Review**：维护者会尽快审查您的代码。根据反馈进行必要的修改。

## 🐞 发现 Bug 或建议

如果您发现了 Bug 或有新的功能建议，但不想直接编写代码，欢迎在 [Issues](https://github.com/xcy960815/openai-apis/issues) 页面提交反馈。

---

再次感谢您的贡献！让我们一起把 **openai-apis** 变得更好！🚀
